DOTFILE_DIR="/workspaces/.codespaces/.persistedshare"
cat "$DOTFILE_DIR/.aliases" >> "$HOME/.aliases"
source "$HOME/.aliases"

# Git config
git config --global alias.co checkout
git config --global alias.cob checkout -b
git config --global alias.s status
